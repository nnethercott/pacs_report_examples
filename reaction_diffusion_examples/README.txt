For both of these simulations you will need to run using "mpirun -np j python3 ./filename.py". 

For the timed_simulation we've provided a few command line arguments as well as implemented mpi reduction operations to aggregate runtimes across processes. This was all handled using mpi4py. 
